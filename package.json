{
  "name": "evolizephoto",
  "version": "1.0.0",
  "description": "Static site hosted on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
